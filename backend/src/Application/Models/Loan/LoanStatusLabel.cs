namespace Biblioteca.Application.Models.Loan;

public static class LoanStatusLabel
{
    public const string ACTIVO = nameof(ACTIVO);
    public const string INACTIVO = nameof(INACTIVO);
    public const string RETRASADO = nameof(RETRASADO);

}
